Revise the content below following this instruction:

{{instruction}}

Rules you must follow:
- Return ONLY the revised content — no preamble, explanation, heading, or markdown code fences
- Preserve ALL HTML tags exactly as written (<strong>, <em>, <a href="...">, <br>, <code>, etc.)
- Do NOT add any new HTML tags
- Modify only the visible text between the HTML tags

Content to revise:
{{content}}
